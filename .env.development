# 仅 npm run dev 时加载：浏览器请求 /api/v1/* 会由 Vite 转发到下方地址（填「源站根」，不要带 /api/v1）
VITE_API_TARGET=https://redspiderbc.cn

# 若代理 HTTPS 时报证书错误（公司代理 / 自签证书），取消下一行注释
# VITE_API_PROXY_INSECURE=1

# 本地直连后端示例（后端在本机 8001 时改用上一行或本行其一）
# VITE_API_TARGET=http://127.0.0.1:8001
