# 后端服务根地址（与 src/api/config.ts 的 API_ORIGIN 一致）
# 开发时优先读 .env.development（npm run dev）；未设置时仍用此处
VITE_API_TARGET=https://redspiderbc.cn

# 生产环境 API 基址请改项目根目录的 .env.production（构建时自动生效），见 src/api/config.ts
