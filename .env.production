# 生产构建（npm run build、update.cmd）会自动加载本文件，无需每次在终端里设变量。
#
# 方案1 — 用 Nginx 把 /api/v1 反代到后端：不要配置 VITE_API_BASE（删除或注释下面整行）。
# 方案2 — 浏览器直连后端（需后端 CORS）：填写完整 API 基址，例如：
VITE_API_BASE=https://redspiderbc.cn/api/v1
